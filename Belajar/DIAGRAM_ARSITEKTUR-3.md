# 📊 DIAGRAM ARSITEKTUR - Sneat Django Project

## 1. Arsitektur Django MVT Pattern

```
┌─────────────────────────────────────────────────────────────┐
│                        BROWSER                              │
│                    (User Interface)                          │
└───────────────────────┬───────────────────────────────────────┘
                        │ HTTP Request
                        │ (GET, POST, PUT, DELETE)
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    URL ROUTING (urls.py)                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  config/urls.py (Main Router)                         │   │
│  │  ├── path('admin/', admin.site.urls)                 │   │
│  │  ├── path('', include('apps.dashboards.urls'))        │   │
│  │  └── path('kpi/', include('apps.kpi_management.urls'))│   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  apps/kpi_management/urls.py (App Router)             │   │
│  │  ├── path('stories/', StoryListView.as_view())         │   │
│  │  ├── path('stories/create/', StoryCreateView.as_view())│   │
│  │  └── path('stories/<int:pk>/', StoryDetailView...)    │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    VIEW (views.py)                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Business Logic                                      │   │
│  │  ├── Get data from Model                             │   │
│  │  ├── Process form data                               │   │
│  │  ├── Validate & Save                                 │   │
│  │  └── Return response                                  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
│  Example:                                                     │
│  class StoryListView(ListView):                              │
│      model = Story                                            │
│      queryset = Story.objects.all()                          │
│      template_name = 'story_list.html'                       │
└───────────────────────┬───────────────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌──────────────┐              ┌──────────────────┐
│   MODEL      │              │    TEMPLATE      │
│ (models.py)  │              │   (.html files)  │
│              │              │                  │
│ Database     │              │  HTML + Django   │
│ Structure    │              │  Template Tags   │
│              │              │                  │
│ Story        │              │  story_list.html │
│ ├── title    │              │  story_form.html │
│ ├── platform │              │  story_detail...│
│ └── views    │              │                  │
└──────┬───────┘              └──────────────────┘
       │
       ▼
┌─────────────────────────────────────────────┐
│          DATABASE (SQLite/PostgreSQL)       │
│  ┌──────────────────────────────────────┐   │
│  │  kpi_management_story               │   │
│  │  ├── id (PK)                        │   │
│  │  ├── title                          │   │
│  │  ├── platform                       │   │
│  │  ├── views                          │   │
│  │  └── created_at                     │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

## 2. Flow Request-Response

```
USER ACTION: Click "Create Story" button
    │
    ▼
┌─────────────────────────────────────────┐
│  Browser sends GET request to:         │
│  /kpi/stories/create/                  │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  URL Router (urls.py)                  │
│  Matches: path('stories/create/', ...) │
│  Routes to: StoryCreateView            │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  View (views.py)                        │
│  StoryCreateView.get()                  │
│  ├── Create empty form                  │
│  ├── Get context data                   │
│  └── Render template                    │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Template (story_form.html)            │
│  ├── Extends layout_path               │
│  ├── Render form fields                │
│  └── Return HTML                        │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  HTTP Response (HTML)                   │
│  Sent back to browser                   │
└───────────────┬─────────────────────────┘
                │
                ▼
        USER SEES FORM

─────────────────────────────────────────────

USER ACTION: Submit form
    │
    ▼
┌─────────────────────────────────────────┐
│  Browser sends POST request            │
│  /kpi/stories/create/                 │
│  + Form data (title, platform, ...)  │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  View (views.py)                        │
│  StoryCreateView.post()                 │
│  ├── Validate form                      │
│  ├── Save to database                   │
│  └── Redirect to success URL            │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Model (models.py)                      │
│  Story.objects.create(...)              │
│  Saves to database                      │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Database                               │
│  New record inserted                    │
└───────────────┬─────────────────────────┘
                │
                ▼
        REDIRECT TO LIST PAGE
```

## 3. Struktur Folder & File

```
sneat-bootstrap-html-django-admin-template-free/
│
├── 📁 config/                          [Konfigurasi Utama]
│   ├── settings.py                     → Pengaturan aplikasi
│   ├── urls.py                         → URL routing utama
│   ├── wsgi.py                         → WSGI config
│   └── template.py                     → Template variables
│
├── 📁 apps/                            [Aplikasi Django]
│   │
│   ├── 📁 authentication/             [Login/Register]
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── templates/
│   │
│   ├── 📁 dashboards/                  [Dashboard Pages]
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── templates/
│   │
│   ├── 📁 kpi_management/              [KPI CRUD System]
│   │   ├── models.py                  → Database structure
│   │   ├── views.py                   → Business logic
│   │   ├── forms.py                   → Form handling
│   │   ├── urls.py                    → URL routing
│   │   ├── admin.py                   → Admin panel config
│   │   └── templates/                 → HTML templates
│   │       ├── story_list.html
│   │       ├── story_form.html
│   │       └── ...
│   │
│   └── 📁 [other apps]/
│
├── 📁 templates/                       [Global Templates]
│   └── layout/
│       ├── master.html                → Base template
│       ├── layout_vertical.html      → Vertical layout
│       └── partials/                  → Reusable components
│           ├── header.html
│           ├── sidebar.html
│           └── footer.html
│
├── 📁 src/                            [Frontend Assets]
│   ├── assets/
│   │   ├── css/                      → Stylesheets
│   │   ├── js/                       → JavaScript
│   │   └── img/                      → Images
│   └── scss/                         → SCSS source files
│
├── 📁 static/                         [Collected Static Files]
├── 📁 media/                          [User Uploaded Files]
│
├── manage.py                          → Django CLI tool
├── requirements.txt                   → Python dependencies
└── db.sqlite3                        → SQLite database
```

## 4. Relasi Database (Model Relationships)

```
┌─────────────────┐
│      User       │ (Django Auth)
│  (django.contrib│
│   .auth.User)   │
└────────┬────────┘
         │
         │ OneToOne
         │
         ▼
┌─────────────────┐
│     Profile     │
│  ├── user (FK)  │
│  ├── role       │
│  └── avatar     │
└────────┬────────┘
         │
         │ OneToMany
         │
         ▼
┌─────────────────┐
│      Story      │
│  ├── created_by │ (FK → User)
│  ├── company    │ (FK → Company)
│  ├── campaign   │ (FK → Campaign)
│  └── collab_brand│ (FK → CollabBrand)
└─────────────────┘

┌─────────────────┐
│    Company      │
│  ├── name       │
│  ├── type       │
│  └── parent     │ (Self FK)
└────────┬────────┘
         │
         │ OneToMany
         │
         ▼
┌─────────────────┐
│    Campaign     │
│  ├── company    │ (FK → Company)
│  ├── owner      │ (FK → User)
│  └── ...        │
└────────┬────────┘
         │
         │ OneToMany
         │
         ▼
┌─────────────────┐
│  CollabBrand    │
│  ├── campaign   │ (FK → Campaign)
│  └── ...        │
└─────────────────┘
```

## 5. CRUD Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    CRUD OPERATIONS                           │
└─────────────────────────────────────────────────────────────┘

CREATE (Create)
    │
    ├── User clicks "Create" button
    ├── GET /kpi/stories/create/
    ├── View: StoryCreateView.get()
    ├── Render: story_form.html (empty form)
    ├── User fills form & submits
    ├── POST /kpi/stories/create/
    ├── View: StoryCreateView.post()
    ├── Validate form
    ├── Save: Story.objects.create(...)
    └── Redirect: /kpi/stories/

READ (List & Detail)
    │
    ├── GET /kpi/stories/
    ├── View: StoryListView.get()
    ├── Query: Story.objects.all()
    ├── Render: story_list.html
    └── Display: Table with all stories
    │
    ├── GET /kpi/stories/1/
    ├── View: StoryDetailView.get()
    ├── Query: Story.objects.get(pk=1)
    ├── Render: story_detail.html
    └── Display: Story details

UPDATE (Edit)
    │
    ├── User clicks "Edit" button
    ├── GET /kpi/stories/1/edit/
    ├── View: StoryUpdateView.get()
    ├── Query: Story.objects.get(pk=1)
    ├── Render: story_form.html (pre-filled)
    ├── User modifies & submits
    ├── POST /kpi/stories/1/edit/
    ├── View: StoryUpdateView.post()
    ├── Validate form
    ├── Update: story.save()
    └── Redirect: /kpi/stories/

DELETE (Remove)
    │
    ├── User clicks "Delete" button
    ├── GET /kpi/stories/1/delete/
    ├── View: StoryDeleteView.get()
    ├── Render: story_confirm_delete.html
    ├── User confirms deletion
    ├── POST /kpi/stories/1/delete/
    ├── View: StoryDeleteView.post()
    ├── Delete: story.delete()
    └── Redirect: /kpi/stories/
```

## 6. Template Inheritance Hierarchy

```
┌─────────────────────────────────────┐
│      master.html (Base Template)   │
│  ├── <head>                         │
│  │   ├── CSS includes              │
│  │   └── Meta tags                 │
│  ├── <body>                         │
│  │   ├── {% block layout %}        │
│  │   └── JS includes                │
│  └── {% block page_css %}          │
└──────────────┬───────────────────────┘
               │
               │ extends
               │
               ▼
┌─────────────────────────────────────┐
│   layout_vertical.html              │
│  ├── {% extends layout_path %}      │
│  ├── Sidebar                       │
│  ├── Header                         │
│  ├── {% block content %}            │
│  └── Footer                         │
└──────────────┬───────────────────────┘
               │
               │ extends
               │
               ▼
┌─────────────────────────────────────┐
│   story_list.html                   │
│  ├── {% extends layout_path %}     │
│  ├── {% block title %}              │
│  ├── {% block content %}            │
│  │   └── Card with table           │
│  └── {% block page_js %}            │
└─────────────────────────────────────┘
```

## 7. Authentication Flow

```
┌─────────────────────────────────────┐
│         Unauthenticated User        │
└──────────────┬──────────────────────┘
               │
               │ Tries to access protected page
               │
               ▼
┌─────────────────────────────────────┐
│   LoginRequiredMixin checks         │
│   → User not authenticated          │
└──────────────┬──────────────────────┘
               │
               │ Redirect to login
               │
               ▼
┌─────────────────────────────────────┐
│      /auth/login/                   │
│      Login Form                     │
└──────────────┬──────────────────────┘
               │
               │ User submits credentials
               │
               ▼
┌─────────────────────────────────────┐
│   View validates credentials        │
│   ├── Check username/password       │
│   ├── Authenticate user             │
│   └── Create session                │
└──────────────┬──────────────────────┘
               │
               │ Success
               │
               ▼
┌─────────────────────────────────────┐
│   Redirect to original page         │
│   or LOGIN_REDIRECT_URL             │
└──────────────┬──────────────────────┘
               │
               │ Now authenticated
               │
               ▼
┌─────────────────────────────────────┐
│   Access granted to protected pages │
└─────────────────────────────────────┘
```

## 8. Form Processing Flow

```
┌─────────────────────────────────────┐
│         Form Submission             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   CSRF Token Validation             │
│   {% csrf_token %}                  │
└──────────────┬──────────────────────┘
               │
               │ Valid
               │
               ▼
┌─────────────────────────────────────┐
│   Form Validation                   │
│   form = StoryForm(request.POST)    │
│   form.is_valid()                   │
└──────────────┬──────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
    Valid          Invalid
        │             │
        │             └──→ Show errors
        │                    Return form with errors
        │
        ▼
┌─────────────────────────────────────┐
│   Process Data                      │
│   form.save()                      │
│   or                               │
│   instance = form.save(commit=False)│
│   instance.created_by = user       │
│   instance.save()                  │
└──────────────┬──────────────────────┘
               │
               │ Success
               │
               ▼
┌─────────────────────────────────────┐
│   Redirect to Success URL          │
│   messages.success(request, ...)   │
└─────────────────────────────────────┘
```

## 9. Static Files Flow

```
┌─────────────────────────────────────┐
│   Development Mode                  │
│                                     │
│   src/assets/css/style.css         │
│   → STATICFILES_DIRS                │
│   → Served directly by Django       │
│   → {% static 'css/style.css' %}    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│   Production Mode                   │
│                                     │
│   python manage.py collectstatic   │
│   → Copies all files to staticfiles/│
│   → Served by web server (nginx)    │
│   → WhiteNoise middleware           │
└─────────────────────────────────────┘
```

## 10. Middleware Stack

```
Request
    │
    ▼
┌─────────────────────────────────────┐
│   SecurityMiddleware               │
│   → Security headers               │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   WhiteNoiseMiddleware             │
│   → Serve static files             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   SessionMiddleware                │
│   → Manage sessions                │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   LocaleMiddleware                 │
│   → Language detection             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   CommonMiddleware                 │
│   → Common operations              │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   CsrfViewMiddleware               │
│   → CSRF protection                │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   AuthenticationMiddleware          │
│   → Add user to request             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   MessageMiddleware                │
│   → Flash messages                 │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   XFrameOptionsMiddleware          │
│   → Clickjacking protection        │
└──────────────┬──────────────────────┘
               │
               ▼
           View
               │
               ▼
           Response
```

---

**Catatan**: Diagram ini memberikan gambaran visual tentang bagaimana komponen-komponen Django bekerja bersama dalam proyek Sneat Django ini.

